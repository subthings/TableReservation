<nav class="navbar navbar-expand-lg navbar-dark site_navbar bg-dark site-navbar-light" id="site-navbar">
    <div class="container">
        <a class="navbar-brand" href="{{ path('index') }}">EatWell</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#site-nav" aria-controls="site-nav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="oi oi-menu"></span> Menu
        </button>

        <div class="collapse navbar-collapse" id="site-nav">
    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            {% for category in categories %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('categoryMenu', { name: category.name }) }}">{{ category }}</a>
                </li>
            {% endfor %}
        <form class="form-inline my-2 my-lg-0">

            {% if is_granted('ROLE_USER') or  is_granted('ROLE_ADMIN') %}
                <a class="nav-link" href="{{ path('ordersList', { id: user.id }) }}">Orders</a>
                <a class="nav-link" href="{{ path('showCart', { id: user.id }) }}">Cart</a>
                <a class="nav-link" href="{{ path('fos_user_security_logout') }}">Log out</a>
            {% else %}
                <a class="nav-link" href="{{ path('fos_user_security_login') }}">Log in</a>
                <a class="nav-link" href="{{ path('fos_user_registration_register') }}">Register</a>
            {% endif %}
        </form>

    </ul>
</div>
</div>
</nav>